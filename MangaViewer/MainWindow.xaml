<Window
    x:Class="MangaViewer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MangaViewer.ViewModels"
    xmlns:converters="using:MangaViewer.Converters"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid.Resources>
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom top title / command bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="32" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="56,0,0,0">
                <Button Command="{Binding TogglePaneCommand}" FontFamily="Segoe MDL2 Assets" Content="&#xE70E;" ToolTipService.ToolTip="썸네일 패널"/>
                <Button Content="폴더 열기" Click="OpenFolder_Click" IsEnabled="{Binding IsOpenFolderEnabled}" Visibility="{Binding IsOpenFolderEnabled, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Content="{Binding DirectionButtonText}" Command="{Binding ToggleDirectionCommand}"/>
                <Button Content="{Binding CoverButtonText}" Command="{Binding ToggleCoverCommand}"/>
                <Button Content="OCR" Command="{Binding RunOcrCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Navigation / content area -->
        <NavigationView Grid.Row="1" x:Name="LeftNav" IsBackButtonVisible="Auto"
                        IsPaneOpen="{Binding IsNavOpen, Mode=TwoWay}" PaneDisplayMode="LeftCompact" OpenPaneLength="240" CompactPaneLength="48"
                        IsTitleBarAutoPaddingEnabled="False" ItemInvoked="LeftNav_ItemInvoked" IsSettingsVisible="True" BackRequested="LeftNav_BackRequested">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="리더" Icon="Library" Tag="Reader"/>
                <NavigationViewItem Content="검색" Icon="Find" Tag="Search"/>
            </NavigationView.MenuItems>
            <NavigationView.PaneFooter>
                <StackPanel Margin="12,12,12,24">
                    <TextBlock Text="Manga Viewer" Opacity="0.6" FontSize="12"/>
                </StackPanel>
            </NavigationView.PaneFooter>
            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
