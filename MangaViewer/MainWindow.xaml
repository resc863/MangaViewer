<Window
    x:Class="MangaViewer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MangaViewer.ViewModels"
    xmlns:converters="using:MangaViewer.Converters"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="Transparent"> <!-- Transparent so acrylic system backdrop shows -->
        <Grid.Resources>
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title bar area becomes acrylic (transparent to backdrop) -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="40" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="10" Margin="10,0">
                <Button Command="{Binding TogglePaneCommand}" FontFamily="Segoe MDL2 Assets" Content="&#xE700;"/>
                <Button Content="폴더 열기" Click="OpenFolder_Click" IsEnabled="{Binding IsControlEnabled}"/>
                <Button Content="{Binding DirectionButtonText}" Command="{Binding ToggleDirectionCommand}"/>
                <Button Content="{Binding CoverButtonText}" Command="{Binding ToggleCoverCommand}"/>
                <Button Content="OCR" Command="{Binding RunOcrCommand}"/>
                <Button Content="설정" Click="OpenSettings_Click"/>
            </StackPanel>
        </Grid>

        <SplitView Grid.Row="1" DisplayMode="Inline" OpenPaneLength="160" IsPaneOpen="{Binding IsPaneOpen, Mode=TwoWay}">
            <SplitView.Pane>
                <!-- Thumbnails: solid background to exclude from acrylic -->
                <ListView x:Name="ThumbnailsList"
                          Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                          ItemsSource="{Binding Thumbnails}"
                          SelectedIndex="{Binding SelectedThumbnailIndex, Mode=TwoWay}"
                          SelectionMode="Single"
                          ContainerContentChanging="ThumbnailsList_ContainerContentChanging">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:MangaPageViewModel">
                            <Image Source="{Binding ThumbnailSource}" Height="120" Stretch="Uniform" Margin="5"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </SplitView.Pane>

            <SplitView.Content>
                <!-- Content area: acrylic (transparent) margins; wrappers solid to exclude images themselves -->
                <Grid Background="Transparent">
                    <!-- Single Page Mode -->
                    <Grid Visibility="{Binding IsSinglePageMode, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid x:Name="SingleWrapper" SizeChanged="OnSingleOcrContainerSizeChanged" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
                            <Image x:Name="SingleImage" Source="{Binding LeftImageSource}" Stretch="Uniform" SizeChanged="OnSingleImageSizeChanged" />
                            <Canvas x:Name="SingleOverlay" IsHitTestVisible="False" />
                        </Grid>
                    </Grid>

                    <!-- Two Page Mode -->
                    <Grid Visibility="{Binding IsTwoPageMode, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="0"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Grid x:Name="LeftWrapper" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" SizeChanged="OnLeftOcrContainerSizeChanged" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
                            <Image x:Name="LeftImage" Source="{Binding LeftImageSource}" Stretch="Uniform" SizeChanged="OnLeftImageSizeChanged" />
                            <Canvas x:Name="LeftOverlay" IsHitTestVisible="False" />
                        </Grid>
                        <Grid x:Name="RightWrapper" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" SizeChanged="OnRightOcrContainerSizeChanged" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
                            <Image x:Name="RightImage" Source="{Binding RightImageSource}" Stretch="Uniform" SizeChanged="OnRightImageSizeChanged" />
                            <Canvas x:Name="RightOverlay" IsHitTestVisible="False" />
                        </Grid>
                    </Grid>
                </Grid>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Window>
